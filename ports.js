'use strict';

//fat arrow function, or lambda
//(arguments) => {body}

const upper = (str) => {
  return str.toUpperCase();
}

const gth = (str) => {
  return "Go To Hell 3:)"
}

module.exports = {
  upper,
  gth
}
